# Production build — set these in the EAS build profile, not in a committed file.
# Values here are PUBLIC: they ship inside the app bundle. Never put a secret in this file.
EXPO_PUBLIC_ENV=production
EXPO_PUBLIC_API_URL=https://api.medpilot.example
