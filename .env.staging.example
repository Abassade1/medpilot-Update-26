# Staging build — copy to .env.staging (or set these in the EAS build profile)
# Values here are PUBLIC: they ship inside the app bundle. Never put a secret in this file.
EXPO_PUBLIC_ENV=staging
EXPO_PUBLIC_API_URL=https://api-staging.medpilot.example
